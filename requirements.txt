# Core dependencies for TalkingMask-Pi5
# Note: lgpio requires system packages (swig, liblgpio-dev) - see install_requirements.sh

sounddevice
soundfile
pyaudio
numpy
pyttsx3
PyYAML
openai
speechrecognition
lgpio
adafruit-circuitpython-servokit
opencv-python
piper-tts

# Optional: Coqui TTS (requires Python < 3.13)
# Install manually if using Python 3.9-3.11: pip install TTS
# Note: Piper TTS is now the default and recommended option for Python 3.13+
